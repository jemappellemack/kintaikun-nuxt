{
  "name": "kintaikun-nuxt",
  "version": "1.0.0",
  "homepage": "https://jemappellemack.github.io/kintaikun-nuxt/",
  "private": true,
  "scripts": {
    "dev": "nuxt-ts",
    "build": "nuxt-ts build",
    "start": "nuxt-ts start",
    "generate": "rm -r docs && nuxt-ts generate && mv dist docs",
    "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt-ts build",
    "generate:gh-pages": "rm -r docs && DEPLOY_ENV=GH_PAGES nuxt-ts generate && mv dist docs"
  },
  "dependencies": {
    "@nuxt/typescript-runtime": "^2.0.0",
    "@nuxtjs/axios": "^5.12.2",
    "@nuxtjs/bulma": "^1.3.0",
    "@nuxtjs/composition-api": "^0.15.1",
    "core-js": "^3.6.5",
    "date-fns": "^2.16.1",
    "date-fns-tz": "^1.0.12",
    "file-saver": "^2.0.5",
    "gh-pages": "^3.1.0",
    "nuxt": "^2.14.6",
    "xlsx": "^0.16.9"
  },
  "devDependencies": {
    "@nuxt/types": "^2.14.6",
    "@nuxt/typescript-build": "^2.0.3"
  }
}
